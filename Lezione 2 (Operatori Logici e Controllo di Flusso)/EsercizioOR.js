// Scrivi un programma che verifichi se un utente può accedere a una funzione speciale se è amministratore oppure se ha attivato un'opzione VIP utilizzando ||.

let administrator = false;
let VIP = true;

if(administrator || VIP){
    console.log("special function allowed.");
}

else{
    console.log("special function denied.");
}

// esercizio svolto praticamente subito.